{
  "name": "gbf-inventory-tracker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types --test src/account/*.test.ts src/capture/*.test.ts src/dashboard/*.test.ts src/planner/*.test.ts src/combat/*.test.ts"
  },
  "devDependencies": {
    "@types/chrome": "0.2.2",
    "@types/node": "26.1.2",
    "typescript": "7.0.2",
    "vite": "8.2.0"
  }
}
