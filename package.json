{
  "name": "gbf-inventory-tracker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types --test src/account/*.test.ts src/capture/*.test.ts src/dashboard/*.test.ts src/passive/*.test.ts src/planner/*.test.ts"
  },
  "devDependencies": {
    "@types/chrome": "latest",
    "@types/node": "latest",
    "typescript": "latest",
    "vite": "latest"
  }
}
